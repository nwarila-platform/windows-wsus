# composition build tree (framework checkout + overlaid role) — never committed
.frameworks/
.quality/
.quality-local.*/

# ansible scratch
*.retry
*.log

# Python/controller environments and caches
.venv/
venv/
**/__pycache__/
*.py[cod]

# Terraform working data and local plans/state
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log

# build worktrees live outside the repo (../.worktrees) but guard anyway
.worktrees/

# local-only secrets / scratch (never commit credentials)
*.secret
temp-password*
*.password
*.pem
*.key
*.pfx
*.p12
